export interface IPluginSettings {
    // defaultColor: c
    // defaultSize: 300,
    // enableResize: false,
    // colors?
}

// - colors
// - defualt color
// - default size
// - enable resize
// - memorize sticky ntoes

export const DEFAULT_SETTINGS: IPluginSettings = {
}